# Macro Shelf-A 0.8.2.0 — 2026-09-21

按用户要求发布当前 0.8.2.0；没有新增修复或悄悄改写本地版执行逻辑。

## 已知问题

用户反馈切换占星后“即刻咏唱”可用，但后续“阳星合相”未执行、四行宏无提示结束。**仍在排查，尚未确认修复**。状态一致性保护不是原生技能名缓存就绪保证；本次发布不代表游戏内验收通过。

## 本版内容

切职状态保护、零默认额外行间隔、完整显式等待、宏组/长宏/倒计时；A+B 界面、独立常用宏栏、用户原图、添加弹窗与紧凑布局修复。205 项逻辑检查及 277 项界面断言通过，游戏边界模拟。

## 发行包与本地包的精确差异

在线 ZIP 中仅 `MacroShelfA.json` 的 `RepoUrl`、`IconUrl` 更新为本合集库地址和版本化图标地址。其余三个文件（DLL、deps.json、icon.png）与已经交付的本地 ZIP 完全相同，运行逻辑未改。

源码 ZIP 为原已验证本地快照，不改写其历史说明或测试证据；其中项目的旧网址可用同目录 `MacroShelfA.json` 的两项发行 URL 覆盖。重现在线打包：对本地插件 ZIP，仅以这里的清单替换同名 JSON，其余条目保持原始字节。无需重编译 DLL。

线上哈希见 `SHA256SUMS.txt`；本地输入与在线输出的对应关系见 `release-verification.json`。安装/更新入口见 [使用说明](../README.md)。
